# 使用回饋紀錄

每一筆回饋一個編號，從提出到結案都留在這裡。技術面的處理寫在 `CHANGELOG.md`，使用者看的說明寫在 `patch-notes.html`。

**狀態**：待評估 → 已排入 26.xx → 已上線 26.xx → 已驗收　／　不採納（要寫理由）
**分類**：介面設計、輸出文件、排程與車次、資料及效能、許願池、其他
（26.02 之前用的是介面／排程／高鐵／輸出／用語／效能／資料七類，FB-001 到 FB-009 維持原分類不回頭改寫）

---

## 目前狀態一覽

| 編號 | 日期 | 來源 | 分類 | 回饋摘要 | 狀態 | 版本 |
|---|---|---|---|---|---|---|
| FB-001 | 2026-08-27 | 使用者 | 排程 | 午餐查詢被鎖死一定要套用 | 已上線 | 26.01 |
| FB-002 | 2026-08-27 | 使用者 | 介面 | 解散地點應可與集合地點不同 | 已上線 | 26.01 |
| FB-003 | 2026-08-27 | 使用者 | 高鐵 | 高鐵班次設定應與集合地點勾稽 | 已上線 | 26.01 |
| FB-004 | 2026-08-27 | 使用者 | 用語 | 「訪視」改「行程」、「廠」改「拜訪單位」 | 已上線 | 26.01 |
| FB-005 | 2026-08-27 | 使用者 | 高鐵 | 需要多組起訖站的車次查詢 | 已上線 | 26.01 |
| FB-006 | 2026-08-27 | 使用者 | 排程 | 塞不下時仍要排完，超時標紅 | 已上線 | 26.01 |
| FB-007 | 2026-08-27 | 使用者 | 介面 | 把看板娘放進版本公告頁的頁首 | 已上線 | 26.02 |
| FB-008 | 2026-08-28 | 使用者 | 高鐵 | 新手指南要寫明高鐵起訖站得自己設 | 已上線 | 26.02 |
| FB-009 | 2026-08-28 | 使用者 | 介面 | 工具裡要有回饋按鈕，就地彈出、可貼截圖 | 已上線 | 26.02 |
| FB-010 | 2026-09-02 | 使用者 | 輸出文件 | 手改行程卡的地址，Google 地圖路線沒有跟著改 | 已上線 | 26.03 |
| FB-011 | 2026-09-02 | 使用者 | 介面設計 | 版本公告頁頁首的三顆按鈕沒有功能 | 已上線 | 26.03 |

---

## FB-001　午餐強制套用

**分類**：排程　**狀態**：已上線 26.01　**提出**：2026-08-27

> 目前午餐的查詢被鎖死一定要套用，希望的因應措施有 2 個。第一，如果我的集合時間已經在中午過後（13:00）就應該要清楚知道這次行程高機率不需要午餐；第二，建議評估設定區域要有選項可以勾選是否需要午餐排程，這也可以回應前述狀況萬一真的有午餐需求也可以重新點選。

**怎麼處理的**

- 「行程日期」每天一個「午餐」勾選框，預設由集合時間自動判斷：**集合時間在 13:00（拜訪單位午休結束）之後就自動不勾**，滑鼠移上去會說明原因
- 使用者手動點過就固定，自動判斷不再蓋掉
- 設定的「午餐條件」多一個「午餐排程：自動判斷／一律排／一律不排」作為全域預設

**要留意**　排程的下界剪枝原本一律把午餐時間算進去，這次跟著條件化了。以後動到那段程式，記得不排午餐的天不能扣午餐時間。

---

## FB-002　解散地點

**分類**：介面　**狀態**：已上線 26.01　**提出**：2026-08-27

> 目前預設解散地點同集合地點，這個預設值本身沒問題，但我希望可以在訪視日期那個區塊製作一個勾選方框確認「解散地點同集合地點」，若取消勾選就開啟解散地點欄位的輸入功能。後我覺得同步驟可以移除「民國格式」這個欄位，並且因應這個改動可能要全面加大工作區的寬度。

**怎麼處理的**

- 「行程日期」多兩欄：「解散同集合」勾選框（預設打勾）與「解散地點」輸入欄（未勾選才能填）
- 移除「民國格式」欄。民國年在每天的行程卡片標題、Word 與列印版都還在，那一欄只是重複顯示
- 工作區寬度 1040px → 1180px
- 解散地點會進入排程計算（最後一段車程改走解散地點），也會出現在公文的「三、集合地點」與 Google 地圖路線的終點

---

## FB-003　高鐵設定與集合地點勾稽

**分類**：高鐵　**狀態**：已上線 26.01　**提出**：2026-08-27

> 「設定」-「高鐵班次」功能，應該要可以先跟「訪視日期」的集合地點勾稽，因為我發生一個使用經驗是台北-台中的高鐵，但是設定沒有更新還維持在預設值。

**怎麼處理的**

- 集合地點或解散地點的文字看起來是某個高鐵站，但沒有任何一組車次的終點對得上時，「行程日期」下方會出現提示，附「把第一組改成 ○○」與「加一組到 ○○」兩顆按鈕
- 每天的車次區也會再提醒一次
- **只建議、不自動改設定**。地址寫法太多樣，自動覆蓋的誤判風險比一鍵套用的麻煩大得多
- 為了避免誤判，只有文字裡出現「高鐵」兩個字才會去推斷車站；否則「臺北市○○路」這種一般地址會被當成臺北站

---

## FB-004　用語改成中性詞

**分類**：用語　**狀態**：已上線 26.01　**提出**：2026-08-27

> 整個專案的「訪視日期」統一改成「行程日期」，因為我沒有要只使用在訪視，還有其他類型的活動也可以使用。
> （追加）專案內提到「工廠」、「廠方」等特定單位名詞，也建議改成「拜訪單位」。

**怎麼處理的**

全專案替換，包含新手指南與系統說明：

| 原本 | 現在 |
|---|---|
| 訪視日期 | 行程日期 |
| 訪視 N 廠 | 行程 N 站 |
| ＋ 空白訪視 | ＋ 空白行程點 |
| 每廠停留 | 每站停留 |
| 廠方午休 | 拜訪單位午休 |
| 五、訪視行程（公文） | 五、行程安排 |
| 訪視行程（預設文件標題） | 行程表 |

**沒有改的**　程式內部的欄位名稱維持原樣，使用者看不到，改了會讓舊案件開不起來。文件裡當成「舉例」的公司全名（例如「◯◯股份有限公司◯◯廠」）也保留，那是地址與名稱的寫法示範。

---

## FB-005　多組高鐵起訖點

**分類**：高鐵　**狀態**：已上線 26.01　**提出**：2026-08-27

> 「設定」-「高鐵班次」功能，目前只能調查一個組起訖點的車次資訊，但是有的時候會遇到出席人員來自不同縣市，這樣就需要不同起訖站的查詢，建議開一個「＋ 加車次」的按鈕。

**怎麼處理的**

- 「高鐵班次」改成路線清單，每一組有自己的名稱、起訖站與兩個緩衝時間，下方一顆「＋ 加車次」，最多六組
- 組名預設「第一組」「第二組」…，可以改成「北部團」「中部團」這種看得懂的名字
- 每天的車次建議、Word、列印、Markdown 都會逐組列出，多組時會標上組名

---

## FB-006　塞不下也要排完

**分類**：排程　**狀態**：已上線 26.01　**提出**：2026-08-27

> 如果使用者提供的行程因故（總長太短、車程過長、站點過多）沒辦法塞進提供的條件內，仍建議先把所有行程都完整排完。例如使用者提供的時間是 0900-1600，但是行程過多導致全部走完要到 1830 才能結束，我希望工具仍把行程安排完成，並在時間甘特圖以紅色警示超出預設時間，這樣使用者就可以手動去調整（例如他就可以去溝通停留的時間縮短，或刪減停靠站）。

**怎麼處理的**

- 最晚結束時間由「硬性上限」改成「軟性目標」。**不會再出現「有 N 家排不進去」**，所有地點一律排完
- 超出目標結束時間的區塊，在時間軸上改成紅色斜紋；超過最晚結束的再加脈動炫光。行程卡片同步紅框
- 時間軸右界跟著行程長度延伸，排到 19:30 也畫得出來；並加上「目標」與「最晚」兩條標線
- 每天的結尾寫清楚超出幾小時幾分，並保留原本「哪一種調整可以回到時限內」的實際試算

**要留意**　這項改動差點讓排程慢六倍。細節寫在 `CHANGELOG.md` 的 26.01，動排程演算法之前務必先讀。

---

## FB-007　看板娘

**分類**：介面　**狀態**：已上線 26.02　**提出**：2026-08-27

> 我這邊有一個看板娘可以使用，後續會擴充，目前你應該可以使用 In the office 這個圖，應該可以放在最上面那個區域。
> （追加）我的角色其實是擬人化鳳凰，而且目前的底圖色系剛好跟 patch-notes 有點不同，能不能調整讓 patch-notes 跟隨看板娘的底圖。

**怎麼處理的**

- 版本公告頁的頁首整條換成 Akari 在辦公室的圖，標題壓在左側暗色漸層上；窄螢幕漸層自動轉成上下向；列印時圖不印、標題轉黑字
- **整頁配色改成跟著底圖走**：深酒紅的底、鳳凰羽翼的金色當重點色。標籤本來靠紅色分辨，底色變紅之後改用金／玉／琥珀／青灰／玫瑰五色
- 只有版本公告頁換色，工具本體與兩份說明維持淺色——那是每天在用的作業畫面
- 順手把 `logo.png` 的鳳凰徽記做成 favicon，四個頁面都換上。徽記以 `data:` URI 內嵌，不必另外上傳檔案

**角色設定**　Akari 是**擬人化鳳凰**，不是朱雀也不是龍。`logo.png` 是龍鳳同框的徽記，取用時取整枚，文案一律寫「鳳凰」。

**素材來源**　`D:\Desktop\desktop\Tools\Character Creation_Akari`。原圖 2.4 MB 對網頁太重，縮成 1600×967、q80 的 `akari-office.jpg`（204 KB）放在網站根目錄。

**後續**　同一組 hero 樣式可以直接換圖沿用；要在主工具 `index.html` 也放，再開一筆回饋。目前刻意不放——主工具是每天在用的作業畫面，頁首放圖會佔掉工作區高度。

---

## FB-008　新手指南要寫明高鐵起訖站得自己設

**分類**：高鐵　**狀態**：已上線 26.02　**提出**：2026-08-28

> 之前有一個使用者的坑，是他沒有自己去「設定」調整高鐵車次的起訖站，導致 output 的站點跟時刻表是錯的。這件事應該更新到 guide。

FB-003 已經在工具裡加了勾稽提示，但 `guide.html` 從頭到尾沒有一句話說「起訖站不會跟著集合地點自動變」。使用者照著指南一路走完也不會知道要去設定看一眼，錯誤還是照樣發生——而且排程與車程都正確，只有車次那一段錯，畫面不報錯，Word 也印得漂漂亮亮。

**怎麼處理的**

只動 `guide.html`，六處：

- 步驟 2「填行程日期」下方加一個警告框：起訖站是另外一組設定、預設臺北 → 左營、工具會提示但**只在地址含「高鐵」兩字時才推斷得出車站**，所以沒看到提示不代表設定是對的
- 五、常見的調整新增「這次換城市：集合地點和高鐵車次要一起改」一節，寫清楚兩個地方各改一次、只改一邊會怎樣
- 設定對照表：「要去台中不是高雄」那列補上「改集合地點不會連動」，並新增「不是從臺北出發 → 改去程起點站」一列
- 全部欄位表的「去程起點站／終點站」說明改寫，點明不會自動連動
- 七、產出之後的「訂高鐵票」加一句「下單前先核對起訖站」
- 八、最常踩的坑由五條變**六條**，新的第 1 條就是這件事（原本五條順延），目次同步

**為什麼不改程式**　FB-003 當時就決定「只建議、不自動改設定」——地址寫法太多樣，自動覆蓋的誤判風險比一鍵套用的麻煩大。這一筆是補文件的缺口，不是推翻那個決定。

**版本**　原本只動文件、不打算跳號，後來 FB-009 同一天進來，兩筆一起併進 26.02。

---

## FB-009　工具裡要有回饋按鈕

**分類**：介面　**狀態**：已上線 26.02　**提出**：2026-08-28

> 我想要一個 Feedback button 在 UI 上，button icon 要重新挑選過，然後做一個符合目前 UI 設計風格的 Feedback 介面。目前我覺得彈出視窗比較好，應該不需要開新分頁或跳轉，這樣可以讓使用者保有當下的使用狀況，甚至他的 feedback 還能方便截圖。然後這個回饋路由資料庫建在公用帳號的 Google Sheet 跟 Drive 儲存空間。

在這之前，使用者要回報只能private message或口頭講，講完常常沒有下文，也沒有編號可以追。FB-001 到 FB-008 全都是這樣進來的。

**怎麼處理的**

- **右下角常駐一顆「意見回饋」按鈕**，按下去是就地彈出的視窗，不換頁、不開新分頁。當下貼好的名單、排好的行程、跳出來的紅字都還在，一邊看一邊寫；截圖 <kbd>Win</kbd>+<kbd>Shift</kbd>+<kbd>S</kbd> 剪完直接 <kbd>Ctrl</kbd>+<kbd>V</kbd> 貼進視窗，也可以拖曳或選檔案，最多 3 張、單張 5 MB
- 表單：滿意度五級、分類六項（介面設計／輸出文件／排程與車次／資料及效能／許願池／其他，預設落在「其他」）、姓名（自動帶入已連線的名字）、內容，以及一個預設打勾的「附上環境資訊」
- 送出後給一組**收件編號**（`R-0001` 起跳），使用者手上有編號可以追
- 後端 `feedback_()`：文字寫進試算表新的「**回饋**」分頁，截圖上傳到同一個帳號雲端硬碟的「JourneyForge 回饋附件」資料夾，分頁只存連結

**icon 為什麼不用聊天泡泡**　參考截圖那顆是圓形藍底的聊天泡泡，那是 SaaS 網站的通用語彙，放進這個通篇方角、細線、墨綠的畫面裡像別的網站黏過來的。改成**便條加一支筆**的線稿，方形、2px 圓角、墨綠底，跟工具列同一套；而且「寫意見」比「跟客服聊天」更貼近這顆按鈕真正會發生的事——沒有人在另一端即時回話。

**環境資訊附了什麼**　版本、瀏覽器 UA、視窗尺寸、這次有幾天／幾站／幾組車次、有沒有案件 ID。**不含單位名稱、地址、出席名單**，視窗裡也照實寫出來，不想附就取消勾選。

**附件為什麼不開共用連結**　回饋截圖十之八九帶著單位名稱與地址。檔案留在公用帳號的資料夾裡，要看就用那個帳號開；真的需要對外再手動開。

**要留意**

- 後端多用了 `DriveApp`，**從舊版更新上來重新部署時會要求重新授權**，要多勾一項雲端硬碟權限
- 「回饋」「使用紀錄」兩個分頁與 Drive 資料夾都是第一次用到時自動建立，資料夾 ID 自動寫回指令碼屬性 `FEEDBACK_FOLDER_ID`，不用人工開
- 貼上事件只在視窗開著時攔截，否則會跟「行程地點整批貼上」搶同一個 `paste`
- 回饋要先連線才送得出去（後端有 `ACCESS_KEY` 閘門）。沒連線時視窗裡會直接給一顆「去填通行碼」的按鈕
- 收件編號用 `getLastRow()` 推算，**手動刪除試算表的列會讓編號重複**。要清資料請整列標記狀態，不要刪

**順手修的**　前端 `logUse()` 一直在呼叫 `gas("log")`，但後端從來沒有這個 action，所以 README 寫的「姓名會記進使用紀錄」其實一筆都沒進去（射後不理，錯誤被 `.catch(()=>{})` 吃掉，沒人發現）。這次補上 `log_()` 與「使用紀錄」分頁。

---

## 待評估

（目前沒有）

## 不採納

（目前沒有）

---

## FB-010　手改地址之後，地圖路線還指著舊地點

**分類**：輸出文件　**狀態**：已上線 26.03　**提出**：2026-09-02

9/24 那天的午餐換了一家，使用者在行程卡上把名稱與地址都改成「回娘家麵食／屏東縣麟洛鄉民族路新庄巷62號」，畫面上看起來完全正確：卡片是新的、時間軸重算了、Word 印出來也是新的。但「在 Google 地圖開啟這天的路線」點下去，中間那個途經點還是舊那家店的位置（瑪家鄉繁華村）。

**這是一個會安靜出錯的 bug，比會報錯的嚴重。** 行程表本身是對的，錯的是要傳給駕駛的那條路線——而唯一會發現的時機是當天真的照著開。

**原因**　卡片上的名稱與地址欄只寫回文字：

```js
d.querySelector(".addr").oninput = e => {
  st.addr = e.target.value;
  if (PLACES[st.ref]) PLACES[st.ref].addr = e.target.value;   // ← lat/lng 沒動
};
```

`PLACES[ref].lat/lng` 還是自動搜出來那家店的座標。而地圖連結、車程推估、跨天距離全部吃座標不吃地址文字：

```js
const at = p => (p && p.lat != null) ? (p.lat + "," + p.lng) : (p.addr || p.name);
```

所以三件事一起錯：地圖途經點、進出這一站的車程推估、以及分天時的距離判斷。

**處理**　新增 `relocate(st)`，掛在名稱與地址欄的 `change`（離開欄位才觸發，不是每打一個字）：

1. 先用 `coarse()` 立刻給一個粗略座標，時間軸馬上能重算
2. 有通行碼就再打一次 `geocode` 升級成精確座標，寫回 `GEO` 快取
3. 定位不精確時標記 `rough`，**地圖連結改用地址原文**——粗略座標丟給 Google 會指到區公所那一帶，不如讓它自己判斷地址
4. 卡片上顯示一行「位置尚未精確定位——地圖連結改用這行地址，車程為粗估」，不再無聲

順手修掉的：手動填好的「午餐（待訂）」占位卡，`pending` 一直是 `true`，**整站會被地圖路線跳過**（`mapsUrl` 刻意不畫未定的午餐）。現在名稱與地址都填了就自動清掉 `pending`。

**車程推估仍然吃粗略座標**，只有地圖連結不吃。粗估的距離再不準，也比 `kmOf` 回 null 之後一律給 20 分鐘準。

---

## FB-011　版本公告頁頁首的按鈕按不動

**分類**：介面設計　**狀態**：已上線 26.03　**提出**：2026-09-02

`patch-notes.html` 頁首那三顆「新手指南／系統說明／← 回到工具」點下去沒有反應。

**原因**　頁首圖片下緣的淡出層漏了一行 `pointer-events:none`：

```css
.hero-fade{position:absolute;left:0;right:0;bottom:0;height:56px;
  background:linear-gradient(180deg,rgba(23,10,13,0),var(--bg))}   /* ← 少了 pointer-events:none */
```

它在 DOM 裡排在 `.hero-body` 後面，所以蓋在最上層，把頁首底部 56px 的點擊全吃掉——而三顆連結剛好就排在那裡。

**為什麼從 26.02 上線到現在都沒被抓到**　`:hover` 不受 `pointer-events` 影響，所以游標照樣變成手指、外框照樣亮起來，看起來一切正常。使用者多半會以為是自己沒點準，再點一次，然後放棄。

**處理**　四個裝飾層（圖片、遮罩、下緣淡出、右下浮水印）一律補上 `pointer-events:none`，`.hero-body` 再墊一層 `z-index:2`，之後就算有人調整 DOM 順序也壓得住。用 `document.elementFromPoint()` 在桌機與窄螢幕兩個寬度各驗過一次，命中的是連結本身。

**同類問題的檢查點**　只要是「絕對定位的裝飾層蓋在可點擊元素上」都有這個風險。這個 repo 裡目前只有 `patch-notes.html` 用這種頁首，其他頁沒有。
