# JourneyForge 交接文件

> 這份文件的用途：把整個開發過程中的決策、現況與踩過的坑寫下來，讓接手的人（或新開的對話）不用從頭問起。
> 最後更新：2026-08-28　｜　目前版本：**26.02**

---

## 一、這是什麼

**行程規劃工具**。使用者貼上要拜訪的單位名稱與地址、指定日期，系統自動分天、決定順序、插午餐、配高鐵車次，並產出公文格式的 Word 或 PDF。

- 使用網址：`https://chrischang01538.github.io/JourneyForge/`
- 儲存庫：`ChrisChang01538/JourneyForge`（**公開**）
- 新手指南：`/guide.html`　系統說明：`/sop.html`　版本公告：`/patch-notes.html`
- 版本規則與發版流程見 `CHANGELOG.md` 開頭；使用回饋見 `FEEDBACK.md`

---

## 二、架構

| 層 | 位置 | 說明 |
|---|---|---|
| 前端 | GitHub Pages | 單檔 `index.html`，**所有排程運算都在瀏覽器內完成**，不需伺服器 |
| 後端 | Google Apps Script | `Code.gs`，部署為網頁應用程式（執行身分「我」、存取「任何人」） |
| 資料庫 | Google 試算表 | 七個分頁，見下 |
| 附件儲存 | Google 雲端硬碟 | 回饋截圖，資料夾由程式自建 |
| 外部服務 | Google Maps Platform | Geocoding API、Places API (New) |

**試算表分頁**

| 分頁 | 內容 | 誰建立 |
|---|---|---|
| 班表 | 高鐵停靠矩陣，一列一班車、十二個站別欄位 | 人工匯入 `班表_全量.csv` |
| 地點快取 | 地址 → 座標，永久保留 | 程式自動 |
| 餐廳快取 | 依約 2 公里網格快取，45 天過期 | 程式自動 |
| 案件 | 每個案子一列，同時存 Markdown 與 JSON | 程式自動 |
| 使用紀錄 | 每個動作一列，管考用 | 程式自動 |
| 回饋 | 使用者從右下角送出的意見，含截圖連結 | 程式自動（26.02 起） |

**認證模型**

- `/exec` 網址寫死在 `index.html` 開頭的 `DEFAULT_GAS_URL`，因 repo 公開故等同公開
- **通行碼是唯一防線**，存在 Apps Script 指令碼屬性 `ACCESS_KEY`，使用者自行輸入並存於個人瀏覽器
- API 金鑰只存在 Apps Script，不出現在前端

**指令碼屬性**

| 屬性 | 必填 | 說明 |
|---|---|---|
| `SHEET_ID` | 是 | 試算表 ID |
| `ACCESS_KEY` | 是 | 通行碼，留空等於不設防 |
| `MAPS_KEY` | 否 | 沒填則無精確定位與午餐搜尋 |
| `TDX_CLIENT_ID` / `TDX_CLIENT_SECRET` | 否 | 設了才能跑 `refreshTimetable()` |

---

## 三、目前狀態

**運作中**

- 前端已部署，功能完整
- 後端在**個人 Google 帳號**下運作，地址定位與午餐搜尋皆已驗證正常
- 班表為台灣高鐵 **2026/2/2 起適用的常態時刻表**，214 班、十二站、含行駛日，由官方 PDF 以座標重建後匯入

**未完成**

| 項目 | 卡在哪 |
|---|---|
| 搬到 Workspace 公用帳號 | Google Cloud 付款驗證需要公司資料，等行政流程 |
| 帳單搬離個人信用卡 | 同上。**目前 API 費用掛在個人卡**，金額趨近於零但需讓主管知悉 |
| TDX 金鑰 | 未申請。班表改用官方 PDF 人工維護，改點時重做 |
| guide.html 的真實截圖 | 目前是 SVG 標註示意圖，可換成實際截圖（每張包在 `<div class="fig">` 裡，換掉 `<svg>` 即可） |

---

## 四、演算法設計決策（改動前務必先讀）

這幾個決定都是踩過錯誤之後才定下來的，看起來可以「優化」的地方通常有原因。

### 分天目標函數

```
成本 = 總車程 + 2 × 超過目標結束時間的分鐘數 + （空轉日 5000）
硬限制：任一天結束時間 > 最晚結束 → 不可行
```

- **曾經是「每動用一天付固定代價 240」**，結果把三天的行程壓成兩天、空掉第三天。原因是那個設計假設「天數可以自己決定，用越少越好」，但**日期是使用者指定的、已經定案的**（高鐵票、行事曆都排好了），空著一天是浪費不是節省。改成罰空轉日。
- **不要改成「平衡負載」**（例如罰閒置時間的平方）。試過，會為了填滿某天而繞遠路，把屏東和阿蓮排在同一天。

### 下界剪枝（效能關鍵）

計算一個組合的最佳順序之前，先檢查：

```
停留時間總和 + 午餐最短時間 + (家數+1) × 10 分鐘  >  當天可用時段？
```

超過就直接判不可行，不再排序。這個判斷**不會誤殺任何可行解**，但效能差異巨大：

| 規模 | 加剪枝前 | 加剪枝後 |
|---|---|---|
| 7 站 3 天 | 2.4 秒 | 0.66 秒 |
| 8 站 3 天 | 12.3 秒 | 1.4 秒 |
| 9 站 3 天 | **48 秒** | 2.9 秒 |

原因是原本會去替「一天跑八家」這種塞不下的組合算全排列（四萬多種），只為得到「不可行」的結論。

### 其他

- **天內順序**：6 家以內全排列，超過用最近鄰 + 2-opt
- **分天**：10 家 5 天以內用精確列舉（DP over subsets），超過退回貪婪
- **午餐**：排程時先用「前後兩站中點」的虛擬餐廳佔位，順序定案後才呼叫 Places 查真實餐廳。這樣避免 N² 次 API 呼叫
- **午休**：12:00–13:00 不排行程，容許上午最後一場延到 12:10。午餐結束時間卡在 13:00
- **午餐可略過**（26.01）：`wantLunch(day)` 決定。每天可以是 `auto`／`on`／`off`，`auto` 的規則是「集合時間已在午休結束之後就不排」。**下界剪枝的 `need` 必須跟著條件化**，否則不排午餐的那天會被誤判成塞不下
- **排不進去時**會實際重跑排程試算四個方向（提早集合／延後結束／縮短停留／加天數），回報哪一種可行。九家以上不試算，成本太高

---

## 五、踩過的坑

按照踩到的順序，每一個都真的發生過。

1. **改 `Code.gs` 一定要重新部署**，存檔不會生效。走「部署 → 管理部署作業 → 鉛筆 → 版本：新版本」，網址會沿用；走「新增部署作業」會產生新網址，得回頭改 `DEFAULT_GAS_URL`。只改指令碼屬性則是即時生效。

2. **匯入 CSV 要取消勾選「將文字轉換為數字、日期和公式」**，不然 `06:15` 會變成時間值。後端有容錯但別依賴它。

3. **API 金鑰的「應用程式限制」要留「無」**。呼叫來自 Apps Script 伺服器不是瀏覽器，設網域限制會擋掉自己。要限制請用「API 限制」，只勾 Geocoding 與 Places (New)。

4. **`run: []` 空陣列在 JavaScript 是 truthy**。班表改成可編輯時給每班補了 `run:[]`，結果 `!t.run` 判斷失效，所有「每日行駛」的班次全被當成不行駛。判斷要寫成 `!t.run || !t.run.length || ...`。

5. **`log` 這個 action 從來沒有在後端存在過**（26.02 才補）。前端 `logUse()` 一直在呼叫 `gas("log")`，但 `doPost` 的 switch 沒有這個 case，而呼叫端寫的是 `.catch(()=>{})` 射後不理，所以錯誤被完全吃掉——文件寫了一年的「使用紀錄」，其實一列都沒寫進去。**射後不理的呼叫要留一條路知道它有沒有成功**，不然壞了沒人會發現。

6. **`.fb-foot` 設了 `display:flex`，`[hidden]` 就失效了**。`[hidden]{display:none}` 是瀏覽器預設樣式，任何 class 的 `display` 都會蓋過它。回饋視窗的成功畫面第一版就是這樣，表單藏起來了但按鈕列還留著。補了 `.fb-panel [hidden]{display:none!important}`。

7. **回饋視窗的 `paste` 會跟「行程地點整批貼上」搶事件**。兩個都掛在 document 上，所以回饋那支開頭要 `if(!fbIsOpen()) return`。

8. **`evalDay` 複製 day 物件時漏了 `place`**，導致演算法一邊用當天的集合地點算停留、一邊用預設值算來回車程。症狀是換城市（例如台中集合跑台中的站）就顯示「排不進去」，明明十分鐘車程。

9. **車程覆寫用臨時 id 當鍵**，換個瀏覽器 id 就對不上。存檔時要把鍵轉成地點名稱、還原時再轉回。

10. **GAS 的 CORS**：前端用 `Content-Type: text/plain` 送 POST 才不會觸發預檢。

11. **第三方時刻表資料不可信**。曾用某網站的資料，0205 次抵達時間差 15 分鐘、1103 次被標成每日行駛（實際只有週一與週六）。一律以官方 PDF 為準。

12. **PDF 文字抽取會丟失欄位對齊**。直接貼進對話的 PDF 文字是壓平的，「該站不停」的橫線被吃掉導致整欄位移。要用座標（pdfplumber）重建才可靠——這也是最後能做出全量班表的關鍵。

13. **前端把 `/exec` 寫死之後**，原本用「有沒有填網址」判斷後端可用性的地方全部失效，沒填通行碼的人會收到一串 API 錯誤而不是乾淨降級。改成 `hasBackend()` 同時檢查網址與通行碼。

---

## 六、維護動作

| 要做什麼 | 怎麼做 |
|---|---|
| 新增使用者 | 給網址與通行碼，不用建帳號 |
| 通行碼外流 | 改指令碼屬性 `ACCESS_KEY`，即時生效，通知全員 |
| 高鐵改點 | 下載官方常態時刻表 PDF，用座標重建成停留矩陣後匯入「班表」分頁；有 TDX 金鑰的話改跑 `refreshTimetable()` |
| 看使用率 | 「使用紀錄」分頁依使用者與時間做樞紐分析 |
| 看產出量 | 「使用紀錄」篩選動作為「產生Word」或「列印」 |
| 追某次產出的內容 | 用案件 ID 到「案件」分頁查，Markdown 欄可直接閱讀 |
| 改前端 | 上傳 `index.html` 覆蓋，一至兩分鐘生效 |

---

## 七、下一步可以做的

排序大致按投資報酬率：

1. **搬到 Workspace 公用帳號**（等公司付款資料）。步驟：新試算表 → 貼 `Code.gs` → 設屬性 → 部署 → 用前端「進階：自訂 Apps Script 網址」先試跑 → 確認後才改 `DEFAULT_GAS_URL`。**先確認該網域允許 Apps Script 部署成「任何人可存取」**，用無痕視窗開 `/exec` 驗證。
2. **申請 TDX**。班表改自動更新，也才有辦法維護全國全量與各站到離站時刻。
3. **Routes API**。目前車程是座標推估，誤差約 ±10–15 分鐘。用「先排序、再只查定案路段」的方式呼叫，三天行程約 12–15 次，成本極低。
4. **真實截圖**替換 `guide.html` 的示意圖。
5. **疏運期間班表**目前完全沒有處理，連假前後要人工查。

---

## 八、要接手這個專案的話

把這份文件貼給新的對話當作背景，然後說明你要做什麼。需要程式碼的話，repo 裡的 `index.html`、`Code.gs` 是完整的，沒有任何外部相依套件——連 .docx 產生器都是自己寫的（.docx 就是 ZIP 包 XML）。

**唯一不在 repo 裡的東西是通行碼與 API 金鑰**，那些在 Apps Script 的指令碼屬性裡。
