# 變更紀錄

寫給開發者、接手的人，以及下一個從零開始的對話。使用者看的版本公告在 `patch-notes.html`，使用回饋清單在 `FEEDBACK.md`。

---

## 版控規則

**版號**：`年份後兩碼.當年度流水號`，例如 `26.01`。跨年從 `01` 重新起算。

- 只有**使用者感受得到的改動**才進版號。改註解、修文件錯字不發版。
- 一版可以包含多筆回饋，版號不因回饋筆數跳號。
- 上線後才發現的急件修正用 `26.01a`、`26.01b`。
- 版號寫在 `index.html` 開頭的 `APP_VERSION` 常數，主畫面右上角徽章由它顯示。**改版時只改那一行。**

**發版流程**

1. 新回饋進 `FEEDBACK.md`，編號 `FB-0xx`，狀態「待評估」
2. 決定納入哪一版，狀態改「已排入 26.xx」
3. 改程式，同步改 `guide.html`／`sop.html`／`README.md` 對應的說明
4. 寫 `CHANGELOG.md`（技術面）與 `patch-notes.html`（使用者面），`APP_VERSION` 跳號
5. `FEEDBACK.md` 狀態改「已上線 26.xx」，驗收後改「已驗收」

不採納的也要留紀錄，狀態寫「不採納」並寫一行原因。這是這套機制最值錢的部分——半年後才不會有人再提一次同樣的事。

**寫版本公告的體例**（`patch-notes.html`）

- 開頭一段「版本概要」，用人話講這版的重點與動機
- 每條掛一個標籤：`✚ 新增`、`▲ 強化`、`▼ 調整`、`✖ 修正`、`⚠ 注意`
- 預設值或行為改變，一律用 `舊值 ⇒ 新值` 的對照寫法

---

## 未發版 — 2026-09-03

錯誤表只收「真正影響使用的平台故障」。`APP_VERSION` 不動——使用者端看得到的行為一個字都沒改，只有後台的錯誤區與後端的錯誤回應變了。

背景：後台「錯誤分組」冒出「1 天超過 17:00」四筆、「1 天超過 17:30」一筆，還有「起訖站代碼不正確」。這些全部是**設計內的正常結果或使用者自己輸入錯**，卻跟真正的故障混在同一張表、同樣標成待處理的「新」，管理者每天要一筆一筆點掉。訊號被雜訊蓋住，這個區塊就等於失效。

**問題有兩層。**

1. `reportErr()` 是唯一的入口，沒有嚴重度也沒有分類。`err_()` 收到什麼都 `appendRow`，狀態欄硬寫 `'新'`；後台只要看到開頭是「新」就給紅底加「標記已處理」按鈕。也就是說：**只要呼叫了 reportErr，就一定變成待辦事項。**
2. `adminErrors_()` 的分組鍵是訊息原文，而訊息裡帶了 `hardDays` 與 `CFG.endMax` 這種每個人都不一樣的值。同一個問題被切成「1 天超過 17:00」「1 天超過 17:30」兩組，次數統計失去意義——分組的用途本來就是「先修次數多的」。

### ▼ 調整

**排程端兩筆 `reportErr` 拿掉。**

- `超過最晚結束時間`：FB-006 之後「最晚結束」已經從硬性上限改成軟性目標，超時是「排完了，請你決定怎麼縮」的正常結果。時間軸標紅、卡片紅框、實際試算的調整建議都還在，只是不再進錯誤表
- `有地點排不進去`：使用者還沒在「行程日期」補日期，畫面上已經指名道姓列出是哪幾個地點了。這是輸入不完整，不是故障

**`adminErrors_()` 分組鍵改成「來源＋動作」。** 訊息原文改放 `sample`（取該組最近一筆）當範例顯示在動作底下。後台表格跟著從「訊息／次數／影響人數／最後發生」改成「來源／動作／次數／影響人數／最後發生」。

### ✚ 新增

**`userErr_(msg)`——使用者輸入問題的專用例外。**

```js
function userErr_(msg) { var e = new Error(msg); e.userError = true; return e; }
```

`doPost` 的 catch 看到 `err.userError` 就在回應裡加 `userError: true`；前端 `gas()` 收到這個旗標只丟例外、不呼叫 `reportErr`。畫面上的提示文字完全不變，使用者該看到的錯誤照樣看得到。

改用 `userErr_` 的八處：`通行碼不正確`、`回饋內容是空的`、截圖 `沒有內容`／`超過 5MB`、`缺少案件 ID`、`這份行程太大`、`找不到這個案件`、`起訖站代碼不正確`。

**維持 `new Error` 的**是真正的平台故障：`MAPS_KEY` 沒設、Places API 失敗、TDX 取權杖／回應失敗、TDX 沒回傳定期時刻表、後端沒設管理者帳密、連不上後端、前端未捕捉例外、`規劃行程` 本身丟出的例外。判斷標準只有一條：**使用者換個輸入就能解決的，不是 bug。**

### ✚ 新增（同批）

**工具列多一顆「開啟案件」，放在「儲存案件」左邊。** 在這之前，要叫回舊案子只有一條路：手上得有整條網址，貼到網址列重新載入。使用者實際手上常常只剩下檔名裡那串代號（`115年度北區訪視計畫_jf20260902-a7b3k9.docx`），卻沒有地方可以輸入它。

- 置中小視窗，一個輸入框。`caseIdFrom()` 連結、純代號都收——`/[?&]case=([^&#\s]+)/` 撈得到就用撈到的，撈不到就當成使用者直接打了代號，再 `decodeURIComponent` 一次
- `?case=` 的載入流程抽成 `openCase(cid, fromUrl)`，網址與按鈕共用同一條路徑，免得兩邊行為長歪。`fromUrl` 只用來決定要不要 `history.replaceState`——從網址進來的本來就對了
- 視窗沿用回饋面板的 `.fb-panel` 外框與內距，只加 `.oc-panel` 把位置改成置中。沒有再寫一套 modal
- 沒填通行碼時，視窗裡直接出現「去填通行碼」，跟回饋面板同一個處理方式
- 提示寫明「開啟之後會取代目前畫面上的行程」。沒有做 `confirm()`——擋在按下去之前的文字比事後跳出來的對話框有用

**右上角版本徽章取消超連結。** `<a href="patch-notes.html">` ⇒ `<span>`，`.verbadge:hover` 一併移除。同一排已經有「版本公告」按鈕，兩個入口指向同一頁，徽章看起來可點反而讓人以為是別的東西。徽章現在就是單純的版本標示。

`APP_VERSION` 維持 `26.03`（本次不跳號）。`guide.html`、`sop.html`、`README.md` 已同步補上「開啟案件」的說明。

### ⚠ 注意

`gas()` 在 `userError` 的情況下會把 `e._logged` 設成 `true`，避免上層 `catch` 的 `if(!e._logged) reportErr("排程","規劃行程",...)` 又把它從另一個入口記回去。之後若有人新增 `reportErr` 的呼叫點，記得沿用同一條原則。

`APP_VERSION` 沒跳號，所以錯誤表的「版本」欄無法區分改動前後的資料。要分辨的話，用 2026-09-03 這個日期切。

---

## 26.03 — 2026-09-02

輸出檔名帶案件代號。`APP_VERSION` 26.02 ⇒ 26.03。

背景：案件代號一直存在（`newCaseId()`，`jf20260902-a7b3k9`），但只出現在網址列與「案件連結」那一格，下載的檔案上完全看不到——三份改過的版本躺在下載資料夾裡長得一模一樣，也沒辦法從檔案回推是哪一件。

**討論過三種標註方式，選了檔名。** 頁尾小字與 `docProps/core.xml` 的 `<cp:keywords>` 都評估過：

- **頁尾**要靠使用者「事後記得刪」，而這個檔案的典型流程是「產生 Word → 改幾個字 → 直接轉寄」，中間沒有任何一步會強迫他回頭看頁尾。指南寫得再清楚也擋不住——第五坑「直接把預設的出席人員發出去」年年有人踩，是同一類錯誤
- **`docProps`** 版面乾淨、不用刪，但使用者不會主動去看，追溯性只對開發者有意義
- **檔名**要拿掉就是改檔名，另存的當下就會看到，而且**公文版面一個字都不用動**

### ✚ 新增

**`safeName()` 與 `outName(ext)`。** `安全化的文件標題 + (CASE_ID ? "_"+CASE_ID : "") + 副檔名`。沒有 `CASE_ID`（沒填通行碼／存檔失敗）就退回原本只有標題的檔名。

**`printAs(name)`：列印前借用 `document.title`。** 「另存為 PDF」的預設檔名取自 `document.title`，改完在 `afterprint` 還回去。`afterprint` 在少數瀏覽器不一定觸發，所以補一個 60 秒的計時器保險，免得分頁標題一直卡在檔名上。

### ▲ 強化

**`btnWord` 與 `btnPrint` 改成先存檔、再產檔案。**

```
產生檔案 → 下載 → saveCase(true)   ⇒   await saveCase(true) → 產生檔案 → 下載
```

`CASE_ID` 是 `saveCase()` 當下才產生的，順序反過來的話**第一次輸出的那一份檔名不會有代號**——而那份通常正是會被轉寄出去的。代價是多一次網路往返的等待；存檔失敗照樣輸出，只是檔名沒有代號。

### ✖ 修正

**手改地址之後，地圖路線／車程推估還指著舊地點（FB-010）**

卡片的名稱與地址欄只寫回文字，`PLACES[ref].lat/lng` 原封不動。地圖連結、車程推估、分天距離全部吃座標不吃文字，所以畫面看起來改好了、Word 也印對了，**只有要傳給駕駛的那條路線是錯的**——而唯一會發現的時機是當天真的照著開。

新增 `relocate(st)`，掛在名稱與地址欄的 `change`（離開欄位才觸發，不是 `input`，不然每打一個字打一次 API）：

1. 先 `coarse()` 給粗略座標，時間軸立刻重算
2. 有通行碼再打一次 `geocode` 升級成精確座標，寫回 `GEO`
3. 不精確就標 `rough`，`mapsUrl()` 的 `at()` 改用地址原文——**粗略座標丟給 Google 會指到區公所那一帶，不如讓它自己判斷地址**
4. 卡片顯示「位置尚未精確定位」一行，不再無聲

連帶：`placeNode()` 也補上 `rough`，集合／解散地點只定位到區層級時同樣改用地址原文。

順手修掉手填的「午餐（待訂）」占位卡 `pending` 一直是 `true`、**整站被地圖路線跳過**的問題（`mapsUrl` 刻意不畫未定的午餐）。名稱與地址都填了就清掉 `pending`。

**車程推估仍然吃粗略座標**，只有地圖連結不吃：粗估的距離再不準，也比 `kmOf` 回 null 之後一律給 20 分鐘準。

**版本公告頁頁首的三顆連結按不動（FB-011）**

`.hero-fade`（頁首圖片下緣淡出到頁面底色的那層）是 `position:absolute; bottom:0; height:56px`，DOM 順序在 `.hero-body` 之後，**沒有 `pointer-events:none`**，所以蓋在最上面把那一帶的點擊全吃掉了。連結剛好排在頁首底部，整組中彈。

**為什麼沒人早點發現**：`:hover` 不吃 `pointer-events`，所以游標會變手指、外框會亮起來，看起來完全正常，只有點下去沒反應。26.02 換上看板娘頁首時就帶進來了。

四個裝飾層（`.hero-img`／`.hero-shade`／`.hero-fade`／`.hero-cap`）一律補 `pointer-events:none`，`.hero-body` 另外墊 `z-index:2`——DOM 順序再怎麼改都壓得住。用 `document.elementFromPoint()` 在 1200px 與 760px 兩個寬度驗過命中的是 `A.back`。

### 文件

- `guide.html`「七、產出之後還要做的事」加一條：對外送出前先看一眼檔名。**沒有升成第七個坑**——選了檔名這條路之後，忘了改的後果只是對方看到一串沒意義的碼，跟「把預設出席人員發出去」不同量級
- `sop.html` 輸出章節補一段檔名規則
- `patch-notes.html` 新增 26.03
- `FEEDBACK.md` 新增 FB-010

### 沒有做的事

- **沒有做「內部版／正式版」兩顆輸出鈕。** 檔名這條路已經把「事後記得刪」變成「另存時看得到」，再多一顆鈕是多一個要解釋的選擇
- **沒有寫進 `docProps`。** 要做的話 `makeDocx()` 要多產 `docProps/core.xml`、`[Content_Types].xml` 加 override、`_rels/.rels` 加關聯，約 10 行。留著，哪天需要「檔名被改掉也追得回來」再說

---

## 後台 26.04 — 2026-09-02

儲存健康度。**工具本身的 `APP_VERSION` 維持 26.02**，一般使用者唯一感受得到的是「案件太大存不進去」時會收到一句人話，其餘都在後台。後端 admin API 由 `1.0` 進到 `1.1`。

動機是兩個看不見的天花板：**單一儲存格 50,000 字元**（案件的 JSON 就存在一格裡）與**單份試算表 1,000 萬格**。前者爆掉是那一件存不進去，後者爆掉是整份試算表一起死——回饋、使用紀錄、錯誤全部寫不進去。原本兩個都沒有任何顯示，會踩到的時候才知道。

### ✚ 新增

**四個指標（`storageHealth_`）**

| 指標 | 黃 | 紅 | 紅了要做的事 |
|---|---|---|---|
| 試算表總格數 | 700 萬 | 900 萬 | 舊的「使用紀錄」「錯誤」搬去另一份試算表封存，刪掉沒用到的空白列 |
| 最大的案件 JSON | 35,000 字元 | 45,000 | JSON 改存雲端硬碟檔案，試算表只留 fileId |
| 案件分頁讀取耗時 | 2,000 ms | 5,000 | `saveCase_`／`loadCase_` 改用 `createTextFinder(id)`，不要整張讀 |
| 案件筆數 | 2,000 件 | 5,000 | 同上；真的很多再依年度拆分頁 |

- 門檻全部是 `Code.gs` 開頭的常數（`CELL_*`／`CELLS_*`／`SCAN_*_MS`／`CASE_ROW_*`），前後台吃同一組值
- **每一項都帶一句 `fix`，後台跟燈號並排顯示。**「燈亮了要幹嘛」必須跟燈號放在一起——三個月後看到紅燈的人不會是寫這段的人
- 總格數算的是 `maxRows × maxColumns`（配置出來的格子），跟 Google 的計算方式一致，所以分頁下方沒用到的空白列一樣佔額度
- 「案件筆數」附帶依近 30 天速度推估的「還有幾個月到紅線」

**`growth_()`：各分頁近 30 天／近 7 天新增列數。** 只讀日期那一欄，不整張讀。光看總列數看不出還剩多少時間，要看斜率。

**後台「資料維運」新增「儲存健康度」與「成長速率」兩張卡片**，排在最上面。燈號沿用既有的 `.dot.ok/.warn/.bad`，比例條另加 `.hbar`，**百分比數字一律並列**，不靠顏色單獨表意。

**總覽頁在非綠燈時壓一條橫幅**（`.banner`／`.banner.warn`），點了直接跳「資料維運」。理由是「資料維運」沒人會每天點進去，而這件事燒起來是整份試算表一起死。

### ▲ 強化

**`saveCase_` 加寫入端長度硬擋。** 超過 `CELL_STOP`（45,000 字元）直接擋下，訊息講清楚三件事：多大、上限多少、可以怎麼辦，並且明講「畫面上的行程還在，不會不見」。

```
Google 原始錯誤（看不懂） ⇒ 這份行程太大（46,120 字元，單一儲存格上限 50,000 字元），
                            存不進去。請減少天數或站數，或拆成兩個案件分開存。
```

**這道檢查刻意做在寫入端而不是只做在後台**——踩到的是使用者，看後台的是管理者，兩者不是同一個人。

### 踩過的坑

- `adminOps_` 量「案件分頁讀取耗時」用的是 `getDataRange().getValues()`，跟 `saveCase_`／`loadCase_` 實際付的成本同一件事，量出來才有意義。讀回來的 rows 直接餵給 `storageHealth_`，**不要讓它自己再讀一次**
- `adminOverview_` 也回一份精簡的 `health`（只有 `level` 與 `alerts`）給橫幅用。它本來就讀過「案件」整張，所以是白撿的；**但不量讀取耗時**——總覽不該為了一條橫幅多付一次整張讀的成本，那一項留給「資料維運」
- `.hbar` 一開始寫 `max-width`，放進 `width:1%` 的 `td` 裡會被壓成 min-content，害各列的比例條長度不能互相比較。改成固定 `width`

---

## 後台 26.03 — 2026-08-28

新增 `admin.html`：管理者專用的後台。**工具本身的 `APP_VERSION` 維持 26.02**——版號規則寫的是「只有使用者感受得到的改動才進版號」，這一版對一般使用者是全透明的，所以後台自己帶一個 `ADMIN_VERSION`，兩條線各走各的。

### ✚ 新增

**後台 `admin.html`**

- 單檔靜態網頁，跟 `index.html` 同一套視覺語言、同樣沒有任何外部相依。圖表是手刻 SVG
- 七個分頁：總覽、使用分析、回饋管理、案件瀏覽、錯誤與健康、成本與配額、資料維運
- **另一組帳密，不吃 `ACCESS_KEY`。** `doPost` 一開頭就把 `admin*` 開頭的 action 分流走。理由：通行碼是發給全體使用者的，拿得到工具就拿得到它，而後台看得到所有人的使用紀錄與回饋內容
- 密碼只存 SHA-256（`ADMIN_HASH` ＝ SHA-256(`ADMIN_SALT`:密碼)），不存明碼。在編輯器執行 `setAdmin('帳號','密碼')` 設定
- 登入發一組 token 放 `sessionStorage`，六小時失效（CacheService 的上限就是六小時，不要試圖設更長）。連續錯 5 次鎖 15 分鐘，每次失敗 `Utilities.sleep(700)` 拖慢連續嘗試

**回饋追蹤**

- 「回饋」分頁多三欄：`追蹤編號`、`處理人`、`處理時間`。用 `ensureCol_()` 補在最後面，不動既有資料
- 後台可直接改狀態與追蹤編號寫回試算表，並可把目前篩選結果匯出成 `FEEDBACK.md` 的體例（一覽表 ＋ 每筆的展開段落）

**錯誤紀錄**

- 新增「錯誤」分頁與 `err` action。前端 `reportErr()` 在四個地方送：`gas()` 的後端失敗、`window.onerror`、`unhandledrejection`、排程的「有地點排不進去」與「超過最晚結束時間」
- **送不出去會先進 `localStorage` 佇列，下次進站補送。** 這是踩過的坑第 5 條的直接對策——`log` 那次就是射後不理把錯誤全吃掉，文件寫了一年的使用紀錄其實一列都沒寫進去
- 同一則訊息一次連線只送一次（`ERR_SEEN`），避免一個迴圈裡的錯誤把分頁洗爆

**用量與成本**

- 新增「用量」分頁，一天一列。`geocode_()` 與 `lunch_()` 收尾時各記一次呼叫數與快取命中數，寫失敗一律吞掉
- 後台依 `PRICE_GEOCODE` / `PRICE_PLACES` 兩個常數估算費用，也算出「快取省下多少」。Google 調價時改那兩行

### ▼ 調整

- `logUse()` 的 `.catch(()=>{})` 保留，但 `gas()` 內部改成失敗時先 `reportErr()` 再往外丟，並在 error 物件上標 `_logged`，避免同一個錯誤被記兩次

### ⚠ 注意

- **「平均停留時間」這個指標做不出來，這一版直接不做。** 使用紀錄記的是離散動作，沒有一段使用的起訖時間。硬要用「同 session 第一筆到最後一筆」推算的話，看完就關掉的人會被算成 0，比沒有還糟。要做得另外埋 session 起訖點
- 後台的健康檢查會**真的各打一次** Geocoding 與 Places，所以做成按鈕、不隨頁面自動跑
- 快取清理只動「地點快取」與「餐廳快取」。**回饋與使用紀錄一律不刪列**——收件編號是用列數推算的
- `admin.html` 開頭的 `GAS_URL` 跟 `index.html` 的 `DEFAULT_GAS_URL` 是同一個，重新部署換網址時**兩邊都要改**

---

## 26.02 — 2026-08-28

處理 FB-007、FB-008、FB-009。前半是純外觀（看板娘、配色、favicon），後半才是這一版真正的重點：**工具裡終於有一條使用者自己送回饋的路**。

### ✚ 新增

**意見回饋（FB-009）**

- `index.html` 右下角常駐 `.fab`，開啟 `.fb-panel`：`position:fixed` 的就地彈窗，**不換頁、不開新分頁**。使用者當下的名單、行程、紅字警示都還在，可以一邊看一邊寫，截圖 <kbd>Ctrl</kbd>+<kbd>V</kbd> 直接貼
- 按鈕 icon 是自繪的「便條＋筆」inline SVG。**刻意不用圓形藍底聊天泡泡**——那是 SaaS 通用語彙，跟這個方角、細線、墨綠的介面打架；而且另一端沒有人即時回話，畫成聊天會給錯期待
- 附件：`paste`／`drop`／`<input type=file>` 三條路都收，最多 3 張、單張 5 MB、總量 10 MB，前後端各擋一次
- **`paste` 只在視窗開著時攔截**（`if(!fbIsOpen()) return`），否則會跟「行程地點整批貼上」搶同一個事件
- 送出後換成成功畫面並顯示收件編號，附「再回饋一則」不用重開視窗
- `.fb-foot` 設了 `display:flex`，會蓋掉 `[hidden]` 的預設樣式，所以補了 `.fb-panel [hidden]{display:none!important}`。**在這個 panel 裡加任何 `display:` 非 `block` 的區塊都要記得這件事**
- `.toast` 的 `z-index` 由 `60` 提到 `90`，否則會被回饋視窗（`70`／`71`）蓋住

**後端 `feedback_()`／`log_()`（FB-009）**

- 「回饋」分頁：`收件編號 | 時間 | 使用者 | 版本 | 分類 | 滿意度 | 內容 | 附件 | 環境 | 狀態 | 處理備註`，狀態預設「待處理」
- 收件編號 `R-0001` 起跳，由 `sh.getLastRow()` 推算。**手動刪列會讓編號重複**，清資料請標記狀態不要刪
- 截圖走 `DriveApp`：試算表儲存格有五萬字上限，base64 圖塞不進去；放 Drive 之後維護者也可以在資料夾裡一次看完所有截圖
- `fbFolder_()` 找不到 `FEEDBACK_FOLDER_ID` 就自己建資料夾並把 ID 寫回指令碼屬性——少一道人工設定，也避免有人手動填錯
- 附件**不開共用連結**。回饋截圖十之八九帶著單位名稱與地址，要看就用公用帳號開
- 順手補了 `log_()`：前端 `logUse()` 從一開始就在呼叫 `gas("log")`，但 `doPost` 沒有這個 case，錯誤被 `.catch(()=>{})` 吃掉，所以 README 寫的「使用紀錄」**一筆都沒寫進去過**

**新手指南補上高鐵起訖站（FB-008）**

- `guide.html` 六處：步驟 2 的警告框、五、常見的調整新增一節、設定對照表兩列、全部欄位表的說明、產出後的核對、最常踩的坑由五條變六條（新的第 1 條就是這件事）
- FB-003 已經在工具裡做了勾稽提示，但指南從頭到尾沒說「起訖站不會跟著集合地點自動變」。**提示只在地址含「高鐵」二字時才推斷得出車站**，所以沒看到提示不等於設定正確——這句話是這次補的重點

### ✚ 新增

**版本公告頁的看板娘頁首**

- `patch-notes.html` 的 `.masthead` 改成 hero：`akari-office.jpg` 以 `object-fit:cover`、`object-position:50% 38%` 鋪滿，上面壓 `.hero-shade` 的左至右暗色漸層讓標題讀得到
- 820px 以下漸層改成上下向（左右向在窄螢幕會讓字壓在人物臉上）
- 列印時隱藏 `.hero-img`／`.hero-shade`／`.hero-cap`，標題轉回黑字
- 圖片來源 `Character Creation_Akari/In the office.jpg`（2648×1600、2.4 MB），縮到 **1600×967、JPEG q80、progressive**，成品 204 KB。**這是唯一需要跟著上傳的新檔案**

**版本公告頁改成鳳凰配色**

- `patch-notes.html` 的 `:root` 換成一組取自看板娘底圖的深色 token：`--bg:#170a0d`／`--panel:#241317`／`--gold:#e8bc6a`，另有 `--jade`／`--amber`／`--rose`／`--slate` 供標籤使用
- **標籤不能再靠紅色分辨**（底色本身就是紅的），`✚▲▼✖⚠` 改用金／玉／琥珀／青灰／玫瑰五色
- `body::before` 放一層 `radial-gradient` 暖光接住頁首；`.hero-fade` 讓圖的下緣淡出到頁面底色
- **只有這一頁換色。** `index.html`／`guide.html`／`sop.html` 維持淺色——那是每天在用的作業畫面
- `@media print` 完整覆寫成白底黑字（含標籤、表格、`.delta`、`.lede`、`.known`），深色頁不會印成一整片黑

**全站 favicon**

- 由 `logo.png` 裁去透明邊、置中補成正方形、縮到 64×64 並量化到 64 色，成品 2.5 KB，以 `data:` URI 直接內嵌在四個頁面的 `<title>` 之後——不必另外上傳檔案，也不會多一次請求

### ⚠ 注意

- **後端多用了 `DriveApp`，重新部署後第一次執行會要求重新授權**，要多勾一項雲端硬碟權限。這是 26.01 更新上來一定會遇到的一步，不是出錯
- 「回饋」「使用紀錄」兩個分頁與 Drive 的「JourneyForge 回饋附件」資料夾都是**第一次用到時自動建立**，不必事先手動開
- 回饋要先連線才送得出去（`ACCESS_KEY` 閘門）。沒連線時視窗裡會給一顆「去填通行碼」的按鈕，不會讓使用者寫完才發現送不出去
- 回饋按鈕**只放在 `index.html`**。`guide.html`／`sop.html`／`patch-notes.html` 沒有通行碼，送出去驗證不過
- 排程、輸出、車次的演算法這一版**完全沒動**
- 之後要再放看板娘的其他圖，沿用同一組 hero CSS 就好，換 `src` 與 `object-position` 即可
- **看板娘 Akari 的設定是擬人化鳳凰**（不是朱雀、不是龍）。`logo.png` 裡龍與鳳同框，取徽記時是取整枚，文案一律寫「鳳凰」
- 配色與看板娘綁在一起了：<b>換底圖就要回頭校一次 token</b>，尤其是 `--gold` 與 `--bg`

---

## 26.01 — 2026-08-27

第一次跑完整套版控流程的版本，同時處理六筆使用回饋（FB-001～FB-006）。

### ✚ 新增

**每天可以決定要不要排午餐**（FB-001）

- 新增 `wantLunch(day)`／`lunchWhy(day)`，`day.lunch` 三態：`auto`／`on`／`off`；設定新增全域 `CFG.lunchMode`（同樣三態）
- `auto` 的規則：`集合時間 >= CFG.lunchTo` 就不排午餐
- 「行程日期」表格每天一個勾選框，初值由 `wantLunch()` 決定；使用者點過就固定成 `on`／`off`
- **`partitionExact()` 的下界剪枝 `need` 必須跟著條件化**（`wantLunch(days[i]) ? lunchMin : 0`）。忘了改的話，不排午餐的那天會被誤判成「塞不下」——這是這項改動唯一會咬人的地方
- `buildDay()` 用 `let lunched=!wantLunch(day)` 起手，等於「已經吃過」，午餐插入邏輯完全不動

**解散地點可以與集合地點不同**（FB-002）

- 新增 `DEST` 與 `setEnds(day)`，與原本的 `ORIGIN`／`setOrigin(day)` 對稱；`setOrigin` 保留為別名，避免遺漏的呼叫點壞掉
- `P(id)` 多認一個 `"dest"`；`schedule()` 最後一段車程由 `leg(prev,"origin")` 改為 `leg(prev,"dest")`
- `day.endSame`（預設 true）與 `day.endPlace`；`isEndSame(day)`／`endPlaceOf(day)` 統一判斷，**舊資料沒有這兩個欄位一律視為「同集合」**
- 連帶改：`legRow` 的回程說法（`endLabel(day)`）、`mapsUrl()` 的終點、`geocodeAll` 前的地點清單、Markdown／列印／Word 的「三、集合地點」（`placeLine(d)`）與回程列
- `legsToNames()`／`legsFromNames()` 的鍵要認得 `dest`，否則換瀏覽器後手動車程對不上（踩過的坑第 6 條）

**高鐵可以開多組起訖站**（FB-005）

- `CFG.originId`／`destId`／`bufOut`／`bufBack` 移出設定，改成 `ROUTES` 陣列（最多六組，`normRoutes()` 正規化）
- `TRAINS` 由單一 `{south,north}` 改成以 `"起站-迄站"` 為鍵的 map；`trainsOf(r)`／`trainsReady(r)`／`anyTrains()`
- `pickSouth(day,r)`／`pickNorth(day,end,r)` 改吃路線參數
- 設定區改成 `renderRoutes()` 動態產生，原本寫死的 `southTable`／`northTable` 與 `addSouth`／`addNorth` 一併移除
- `loadTimetable(one)` 逐組向後端要資料，**`Code.gs` 不用改**（`timetable` 本來就吃 `originId`／`destId`）
- 輸出的車次表在多組時會在「去程／回程」後面加上組名

**集合地點與車次設定的勾稽**（FB-003）

- `guessStationId(text)`：**只在文字含「高鐵」時才推斷車站**，否則「臺北市」這種一般地址會被誤判成臺北站
- 「行程日期」下方與每天的車次區顯示不一致提示，附「把第一組改成 ○○」與「加一組到 ○○」兩顆按鈕
- 一律只建議、不自動覆蓋設定

### ▼ 調整

**排不進去不再把地點丟著不排**（FB-006）

```
最晚結束時間：硬性上限（超過就不排） ⇒ 軟性目標（超過照排，紅色警示）
```

- 新增 `placeLeftovers(days,out)`：`partitionExact()` 先照原本的規則求出「不超時的最佳分天」，剩下的站再逐一補進代價最小的那天。補位一定排完，`left` 只在完全沒有日期可放時才有東西
- 補位的評分是 `(增加的時間) + overCost(end, endTarget, endMax)`，`overCost` 為超目標每分鐘 2、超最晚每分鐘 20
- `bestOrder(day,ids,cheap)` 多一個 `cheap` 參數，補位時跳過全排列、直接用最近鄰＋2-opt
- **`partitionExact()` 的下界剪枝與硬限制維持 26.00 的寫法**。曾經改成「超時也照算」，結果 8 站 3 天由 0.48 秒變成 2.9 秒、9 站由 0.96 秒變成 7.8 秒——原因是那些塞不下的組合每一個都要跑 2-opt。先快速排、再補位，效能與 26.00 相同
- `feasible()` 更名 `fitsWithin()`，語意由「排不排得進去」改成「調整之後會不會超時」
- `suggestFix()` 只在**超過最晚結束**時才跑（26.00 是排不進去才跑）。曾經改成超過目標就跑，結果每次小幅超時都要重跑十幾次完整分天，8 站 3 天直接慢六倍
- 時間軸右界由固定 18:30 改成 `max(18:30, max(結束, 最晚結束)+30)`，並加上「目標」（橘虛線）與「最晚」（紅實線）兩條標線；超時的區塊改紅色斜紋，超過最晚結束的再加脈動炫光
- 行程卡片超時時紅框，超過最晚結束的加同一組炫光（`@keyframes glow`，`prefers-reduced-motion` 下自動停用）

**全專案用語改成中性詞**（FB-004）

```
訪視日期 ⇒ 行程日期        訪視 N 廠 ⇒ 行程 N 站
＋ 空白訪視 ⇒ ＋ 空白行程點   每廠停留 ⇒ 每站停留
廠方午休 ⇒ 拜訪單位午休      五、訪視行程 ⇒ 五、行程安排
預設文件標題：訪視行程 ⇒ 行程表
```

- 程式內部的 `kind:"visit"` **維持原樣**。使用者看不到，改了會讓舊案件的 JSON 對不上
- `guide.html`／`sop.html`／`README.md`／`HANDOFF.md` 一併改

**其他**

- 工作區寬度 `1040px ⇒ 1180px`（「行程日期」多了兩欄，原寬度會擠）
- 移除「行程日期」的「民國格式」欄。民國年在每天的卡片標題、Word 與列印版都還在（`rocLabel()`），這一欄只是重複顯示
- 每日結尾的狀態標籤由「有衝突／時段無衝突」改成「壓到午休／午休時段淨空」。超時已經有自己的紅色標籤與說明，混在一起看不出是哪一種問題

### ✖ 修正

- **班次編輯區的方向標題顯示 `undefined → undefined`**。`renderTrains()` 讀的是 `CFG.depStation`／`CFG.hubStation`，這兩個欄位在 `defCfg()` 裡根本不存在。改寫成 `renderRoutes()` 時一併修掉，改用 `stName(r.originId)`／`stName(r.destId)`
- **`toHM()` 超過 24 時會繞回 `00:xx`**。以前排程有硬性上限碰不到，現在超時照排就看得到了，`25:10` 才看得出是排到隔天凌晨

### ⚠ 注意（相容性）

| 舊資料 | 處理 |
|---|---|
| 26.00 的 `localStorage` | `load()` 把 `CFG.originId`／`destId`／`bufOut`／`bufBack` 搬成 `ROUTES[0]`，舊的單一 `TRAINS` 搬成 `TRAINS["1000-1070"]`，然後刪掉 CFG 裡的舊鍵 |
| 26.00 存的案件 JSON（`v:1`） | 沒有 `routes` 就用 `originId`／`destId` 組出一組；沒有 `endSame`／`lunch` 的日期一律視為「解散同集合、午餐自動判斷」 |
| 新版存的案件（`v:2`） | 仍會寫入 `originId`／`destId`，萬一有人開著舊版分頁也讀得到主要路線 |

**沒有任何一種舊資料需要手動轉檔。**

### 效能

`node + chromium` 實測，同一台機器、同一組測資（高雄／屏東九個點、三天）：

| 規模 | 26.00 | 26.01 |
|---|---|---|
| 7 站 3 天 | 0.23 秒 | 0.24 秒 |
| 8 站 3 天 | 0.48 秒 | 0.51 秒 |
| 9 站 3 天 | 0.96 秒 | 0.92 秒 |

分天結果與 26.00 完全相同（3/2/2、3/3/2、3/3/3）。

> 這組數字與 `HANDOFF.md` 裡的 2.4／12.3／48 秒不是同一台機器，只能拿來比較同一列的前後，不能跨表比。
>
> **改動排程演算法時務必重測。** 方法：開兩個瀏覽器分頁分別載入新舊版的 `index.html`，在主控台把同一組地點灌進 `ITEMS`、同樣三天灌進 `DAYS`，然後量 `await run()` 的耗時——
>
> ```js
> const t0=performance.now(); await run(); console.log(Math.round(performance.now()-t0)+' ms');
> ```
>
> 測資要固定（我用高雄／屏東九個點、三天、預設設定），而且要比對分天結果有沒有跟著變。

---

## 26.00 — 基準版

版控機制建立之前的既有功能，不回頭補寫歷史。功能範圍以當時的 `README.md` 與 `HANDOFF.md` 為準。
